# Dev Plans

This directory keeps example plan files for contributors who want to work with the `.claude` helper workflow.

The public repository no longer includes the project's old internal planning history, but it still keeps a minimal example here so the expected structure is visible.

Start with:

- `000-example-dev-plan.md`

Use the example as a template for new implementation or cleanup plans.
